{
  "name": "pedigree-builder",
  "version": "1.0.0",
  "description": "Een webtool voor het bouwen en beheren van hondenstambomen",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "vercel-build": "npm run install:all && npm run build"
  },
  "keywords": ["pedigree", "dogs", "family-tree", "supabase"],
  "author": "Jordy Lissenburg",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
